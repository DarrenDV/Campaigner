Stappenplan:

XXX Check which gameobject is closest to the current gameobject (might still have to check snappoints)
XXX Only check the snappoints of this gameobject

XXX Get closest snappoint of that gameobject
XXX Check which snappoint of own gameobject is closest to that gameobject

Set own position to position of other snap point
Offset own position so that own snappoint aligns with other snap point
